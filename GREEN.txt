M15 绿灯记录（2026-08-27）

命令：./scripts/test.sh
结果：通过
- pytest：202 passed（2 条既有弃用 warning）
- vitest：8 个测试文件、25 个测试通过（有既有 React act warning）

附加检查：
- .venv/bin/python -m ruff check .：通过
- cd apps/web && npx tsc --noEmit：通过

说明：当前环境没有 make，故无法直接执行 make lint；以上两条是 Makefile 中 lint
目标的原始等价命令，均已通过。

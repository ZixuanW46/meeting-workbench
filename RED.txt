M15 红灯记录（2026-08-27）

命令：.venv/bin/python -m pytest tests/api/test_transcode.py -q
结果：失败（6 failed, 3 passed）

失败原因均为缺少 M15 实现：
- MP3/M4A/AAC multipart 上传仍按原文件保存，没有调用 ffmpeg。
- tus 完成时没有调用 ffmpeg。
- PATH 中缺少 ffmpeg 时仍返回 200，没有返回 422。
- ffmpeg stub 非 0 退出时仍返回 204，没有返回 422。

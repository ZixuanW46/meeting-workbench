# scripts/

运维与启动脚本，按 ROADMAP 逐步补齐：

- M12：`download_models.md`（Will 手动下载模型权重的指引）、`smoke_real_models.sh`
- M13：`mac_install.sh`、`backup.sh`、`restore.sh`、`launchd/`

M12 的模型权重由 Will 按 `download_models.md` 手动准备；代码和脚本都不会下载权重。
